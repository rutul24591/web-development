/** Wrapping a folder name informs next js to tried the route group to exclude from routes url path.
 * Name will be omitted from url path. Route groups helps to organize our code
 */
import React from 'react';

const Login = () => {
	return <div>Login Page</div>;
};

export default Login;
