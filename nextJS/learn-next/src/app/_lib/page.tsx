/** _nameoffolder indicated private route which next won't serve.
 * So _ at start of folder name serves to create private route making it reserve.
 * To have a folder that starts with an '_' use %5F instead.
 */
import React from 'react';

const PrivateRoute = () => {
	return <>Private Route</>;
};

export default PrivateRoute;
